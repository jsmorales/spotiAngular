import { ImageValidatorPipe } from './image-validator.pipe';

describe('ImageValidatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageValidatorPipe();
    expect(pipe).toBeTruthy();
  });
});
